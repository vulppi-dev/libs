# useIMask

```tsx
const Component = forwardRef((props, forwardRef) => {
  const ref = useIMask(
    {
      /* IMask options */
    },
    forwardRef /* forward ref is optional */,
  )

  return <input ref={ref} />
})
```
